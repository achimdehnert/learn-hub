# .ship.conf — Deployment SSOT (ADR-120)
APP_NAME="learn-hub"
IMAGE="ghcr.io/achimdehnert/learn-hub/learn-hub-web"
DOCKERFILE="Dockerfile"
WEB_SERVICE="web"
SERVER="root@88.198.191.108"
COMPOSE_PATH="/opt/learn-hub"
COMPOSE_FILE="docker-compose.prod.yml"
HEALTH_URL="https://learn.iil.pet/healthz/"
MIGRATE_CMD="python manage.py migrate --no-input"
